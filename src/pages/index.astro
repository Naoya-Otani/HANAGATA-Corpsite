---
import Base from "@/layouts/Base.astro";
import Banner from "@/layouts/components/Banner.astro";
import OurMission from "@/components/OurMission.astro";
import OurValue from "@/components/OurValue.astro";
import Testimonial from "@/layouts/components/Testimonial.astro";
import Service from "@/layouts/components/Service.astro";
import Cta from "@/layouts/partials/Cta.astro";
import { getEntryBySlug } from "astro:content";

const homepage = await getEntryBySlug("homepage", "index");
const { 
  banner,
  key_features,
  our_values,
  service
  } = homepage.data;
---

<Base>
  <!-- banner -->
  <Banner banner={banner} />
  <!-- key features -->
  <OurMission key_features={key_features} />
  <OurValue our_values={our_values} />
  <!-- service -->
  <Service service={service} />
  <!-- testimonial -->
  <!-- <Testimonial testimonial={testimonial} /> -->
  <!-- call to action -->
  <Cta />
</Base>
